# مستندات توسعه‌دهندگان — مدرسه هوش مصنوعی یاس

توسعه و نگهداری توسط **جعفر قربانی** / **مدرسه هوش مصنوعی یاس**. این سند جزئیات فنی سازوکار وب‌سایت گالری، خط لوله داده‌ها (Data Pipeline) و نحوه مدیریت استایل‌ها را تشریح می‌کند.

- **آدرس مخزن پروژه:** [https://github.com/hvrashta/nps](https://github.com/hvrashta/nps)

## 🏗️ ساختار و معماری کلی

این پروژه یک **سایت استاتیک مبتنی بر داده (Data-Driven Static Site)** است. به جای هاردکد کردن داده‌ها درون HTML، سایت مستقیماً از فایل‌های YAML استایل و تصاویر پیش‌نمایش در ریشه مخزن ساخته می‌شود.

- `/styles/*.yaml`: منبع اصلی و معتبر برای پرامپت‌ها و ساختار استایل‌ها.
- `/preview/[style_id]/*.webp`: تصاویر پیش‌نمایش هر استایل.
- `/src/`: کدهای منبع فریم‌ورک Astro (صفحات، چیدمان‌ها، کامپوننت‌ها).
- `/src/scripts/`: منطق فرانت‌اند ماژولار:
  - `app.js`: ارکستراسیون اصلی، دریافت داده‌ها و فیلترها.
  - `i18n.js`: مدیریت سیستم چندزبانه با پشتیبانی پیش‌فرض از فارسی (`fa`) و RTL.
  - `theme.js`: مدیریت حالت تیره و روشن (Dark/Light Mode).
  - `navigation.js`: منوی شناور و ناوبری دسته‌بندی‌ها.
  - `ui/`: کامپوننت‌های رابط کاربری (کارت استایل، شیت مشخصات، مدال اشتراک‌گذاری، تولتیپ).
  - `utils.js`: توابع کمکی مشترک (پیام‌های Toast، نرمال‌سازی مسیرها).
- `/public/data/styles.json`: پایگاه‌داده ساختاریافته خروجی که توسط فرانت‌اند مصرف می‌شود.

## ⚙️ خط لوله داده‌ها (Data Pipeline)

### ۱. همگام‌سازی استایل‌ها (`scripts/build-data.js`)
این اسکریپت اصلی بیلد پروژه است که عملیات زیر را انجام می‌دهد:
- خواندن فهرست مطالب `README.md` برای استخراج دسته‌بندی‌ها.
- خواندن تمامی فایل‌های `.yaml` در پوشه `/styles`.
- اسکن پوشه `/preview` برای یافتن تصاویر مرتبط.
- **فیلتر استایل‌ها**: تنها استایل‌هایی که دارای تصویر پیش‌نمایش باشند در سایت نهایی گنجانده می‌شوند.
- **مدیریت دارایی‌ها**: انتقال پوشه‌های تصاویر از ریشه مخزن به `/public/preview`.
- **تولید فایل نهایی**: ساخت فایل `public/data/styles.json`.

**اجرای دستی:** `npm run build:data`

### ۲. بهینه‌سازی و نام‌گذاری تصاویر (`scripts/rename-previews.py`)
ابزار پایتون برای یکسان‌سازی نام‌گذاری تصاویر پیش‌نمایش در پوشه‌ها.
- تصاویر را به الگوی `{folder_name}_{index}.{ext}` تغییر نام می‌دهد.

**اجرای دستی:** `python scripts/rename-previews.py`

## 🛠️ توسعه محلی (Local Development)

۱. **نصب وابستگی‌ها**: `npm install`
۲. **اجرای سرور توسعه**: `npm run dev`
۳. **اجرای آزمون‌ها**: `npm test`

## 🌐 سیستم چندزبانه (i18n)

سایت دارای سیستم چندزبانه سبک و بومی است:
- **`src/scripts/i18n.js`**: حاوی واژه‌نامه‌ها و منطق تغییر زبان.
- **اتصال به DOM**: المان‌های دارای مشخصه `data-i18n="key"` به طور خودکار ترجمه می‌شوند.
- **پشتیبانی از دسترس‌پذیری**: مشخصه `data-i18n-aria="key"` برچسب `aria-label` را به‌روزرسانی می‌کند.
- **ماندگاری تنظیمات**: زبان انتخابی در `localStorage` مرورگر کاربر ذخیره می‌شود.

## 🌓 تم تیره و روشن

از طریق `src/scripts/theme.js` مدیریت می‌شود:
- استفاده از CSS Custom Properties برای رنگ‌ها و متغیرها.
- احترام به تنظیمات سیستم‌عامل کاربر و ذخیره انتخاب در `localStorage`.
- تغییر نرم و روان تم با کلیک روی دکمه تغییر حالت.

## 🎨 سیستم طراحی (Design System)

فرانت‌اند با **Astro** و **CSS مدرن** پیاده‌سازی شده است:
- **تایپوگرافی**: فونت استاندارد و خوانای **وزیرمتن (Vazirmatn)** به همراه Inter و JetBrains Mono.
- **طراحی مدرن**: استفاده از شیشه‌مورفیسم (Glassmorphism)، گرادینت‌های ملایم یاس، و سایه‌های چندلایه.
- **ریسپانسیو**: ساختار شبکه‌ای Bento Grid و پنجره‌های اختصاصی موبایل.

## 📜 استاندارد پیام‌های کامیت Git

برای حفظ نظم تاریخچه پروژه، ساختار پیام‌ها باید مطابق فرمت زیر باشد:
`type (Scope) - Imperative description`

**نمونه‌ها:**
- `feat (i18n/UI) - Add Persian translations`
- `fix (UI/Card) - Correct animated border on mobile`
- `brand (Styles) - Update Yas AI School palette`
- `docs (Dev) - Update Persian developer documentation`

## 🚀 استقرار (Deployment)

سایت به عنوان Static Site (SSG) توسط Astro کامپایل می‌شود:
- بیلد: `npm run build`
- خروجی: پوشه `dist/`
- **تنظیمات استقرار Vercel**:
  - فریم‌ورک: Astro
  - دستور بیلد: `npm run build`
  - دایرکتوری خروجی: `dist`


