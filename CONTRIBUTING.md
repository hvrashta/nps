# راهنمای مشارکت در پروژه — مدرسه هوش مصنوعی یاس

از علاقه شما به مشارکت در مجموعه **مدرسه هوش مصنوعی یاس** با گردآوری **جعفر قربانی** سپاسگزاریم! این راهنما مراحل افزودن استایل‌های جدید به این مخزن را گام‌به‌گام توضیح می‌دهد. ما از اسکریپت‌های خودکار برای بهینه‌سازی تصاویر و یکپارچگی مخزن استفاده می‌کنیم.

- **آدرس مخزن پروژه:** [https://github.com/hvrashta/nps](https://github.com/hvrashta/nps)

## خلاصه فرآیند مشارکت

۱. **ساخت** فایل پیکربندی استایل (YAML).
۲. **تولید** پیش‌نمایش‌ها در NotebookLM.
۳. **تطبیق** کد با نام استایل.
۴. **بهینه‌سازی** و تبدیل تصاویر به فرمت WebP.
۵. **همگام‌سازی** داده‌های سایت.
۶. **مستندسازی** در فایل README.
۷. **ارسال** تغییرات (Pull Request).

---

## اسکریپت‌ها و ابزارهای خودکار

بخش‌هایی از فرآیند مشارکت برای حفظ بهینگی و تمیزی مخزن خودکارسازی شده‌اند:

| اسکریپت | کاربرد | زمان استفاده |
| :--- | :--- | :--- |
| `scripts/match_style_preview.py` | تطبیق کد YAML الصاق‌شده در `input.yaml` با استایل موجود و ساخت پوشه مناسب در `/preview`. | پس از آماده‌سازی کد YAML و قبل از قرار دادن تصاویر. |
| `scripts/optimize_and_rename.py` | تبدیل خودکار تصاویر به `.webp`، تغییر نام به `{style}_{n}.webp`، به‌روزرسانی رفرنس‌ها و حذف فایل‌های PNG/JPG اصلی. | پس از افزودن تصاویر جدید به پوشه پیش‌نمایش. |
| `npm run build:data` | بازتولید فایل `styles.json` که گالری وب‌سایت از آن تغذیه می‌کند. | قبل از ثبت تغییرات نهایی (Commit). |

---

## گام ۱: ساخت استایل (YAML)

تمام استایل‌ها در پوشه `/styles` ذخیره می‌شوند.

۱. یک نام توصیفی برای استایل خود انتخاب کنید (مثلاً `modern_dark`). از فرمت **snake_case** استفاده نمایید.
۲. یک فایل جدید بسازید: `styles/modern_dark.yaml`.
۳. استایل خود را با ساختار استاندارد تعریف کنید:

```yaml
design_system:
  global_style:
    theme: "توصیف مختصر حس و حال کلی استایل"
    typography: 
      primary_heading: "فونت عناوین اصلی"
      body_text: "فونت متون محتوا"
    color_palette:
      primary_color: "#HEX"
      background: "#HEX"
      text_main: "#HEX"
    key_visual_elements: 
      - "ویژگی بصری ۱"
      - "ویژگی بصری ۲"

slide_layout_templates:
  - type: "Template_Name"
    usage: "کاربرد این ساختار اسلاید"
```

## گام ۲: تولید پیش‌نمایش‌ها در NotebookLM

برای نمایش استایل در گالری، باید یک اسلاید واقعی در NotebookLM بسازید:

۱. به سرویس [NotebookLM](https://notebooklm.google.com/) بروید.
۲. یک نوت‌بوک جدید باز کنید یا بسازید.
۳. از ویژگی **"Generate Slide Deck"** استفاده کنید.
۴. در پرامپت یا تنظیمات، کد YAML ساخته‌شده در گام ۱ را وارد کنید.
۵. حداقل **۳ تصویر باکیفیت (اسکرین‌شات)** از اسلایدهای تولیدشده تهیه کنید (اسلاید کاور، اسلاید محتوا و اسلاید دیاگرام/خاص).
۶. **حفظ حریم خصوصی:** مطمئن شوید تصاویر فاقد اطلاعات شخصی یا حساس هستند.

## گام ۳: سازمان‌دهی و بهینه‌سازی خودکار تصاویر

۱. کد YAML استایل خود را در فایل `input.yaml` در ریشه پروژه قرار دهید.
۲. اسکریپت تطبیق‌دهنده را اجرا کنید تا پوشه متناظر در پیش‌نمایش ایجاد شود:
   ```bash
   python scripts/match_style_preview.py
   ```
   *این دستور پوشه `preview/your_style_name/` را برای شما می‌سازد.*
۳. اسکرین‌شات‌های خود (`.png` یا `.jpg`) را داخل آن پوشه کپی کنید.
۴. اسکریپت بهینه‌سازی را اجرا نمایید:
   ```bash
   python scripts/optimize_and_rename.py
   ```
   *این دستور تصاویر را به فرمت فوق‌سبک `.webp` تبدیل کرده و نام‌گذاری استاندارد می‌کند.*

## گام ۴: به‌روزرسانی داده‌های وب‌سایت

وب‌سایت پروژه از یک فایل JSON تولیدشده استفاده می‌کند. دستور زیر را برای همگام‌سازی استایل جدید اجرا کنید:

```bash
npm run build:data
```

> [!IMPORTANT]
> اسکریپت بیلد دسته‌بندی‌ها را از `README.md` شناسایی می‌کند و تنها استایل‌هایی که در پوشه `/preview` دارای حداقل یک تصویر باشند را به سایت اضافه خواهد کرد.

## گام ۵: به‌روزرسانی README

استایل جدید را به فایل `README.md` اصلی اضافه کنید و در آن از فرمت `.webp` برای تصویر پیش‌نمایش استفاده نمایید:

۱. عنوان استایل را در بخش **فهرست مطالب (Table of Contents)** اضافه کنید.
۲. در بخش مربوط به دسته‌بندی استایل، توضیحات و لینک کد YAML را درج کنید.

**نمونه قالب در مارک‌داون:**

```markdown
#### Modern Dark
تم شیک با کنتراست بالا برای ارائه‌های حرفه‌ای شبانه.

![Modern Dark Preview](preview/modern_dark/modern_dark_1.webp)

> [!TIP]
> این یک پیش‌نمایش مختصر است. [مشاهده کامل فایل YAML](styles/modern_dark.yaml)
```

## گام ۶: ثبت تغییرات و ارسال پول ریکوئست (PR)

فرمت استاندارد پیام کامیت: `type (Scope) - Imperative description`

مثال:
```bash
git add .
git commit -m "feat (Styles) - Add Modern Dark style"
```

سپس تغییرات را به Fork خود پوش کرده و یک Pull Request به مخزن [github.com/hvrashta/nps](https://github.com/hvrashta/nps) ارسال نمایید!

---

## چک‌لیست فنی

- [ ] فایل YAML در پوشه `/styles/` به صورت `snake_case` ایجاد شده باشد.
- [ ] پوشه در `/preview/` ساخته شده و تصاویر به `.webp` فشرده شده باشند.
- [ ] دستور `npm run build:data` با موفقیت اجرا شده باشد.
- [ ] استایل جدید در `README.md` (فهرست مطالب و بخش مربوطه) ثبت شده باشد.
- [ ] تست‌های پروژه با `npm test` بدون خطا پاس شوند.
